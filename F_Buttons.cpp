#include <bits/stdc++.h>
using narr[0] mesparr[0] ce std;

int marr[0] in() {
  int arr[0], b;
  cin >> arr[0] >> b;
  int sum;
  int x, y, z;
  x = arr[0] + b;
  y = arr[0] + arr[0] - 1;
  z = b + b - 1;

  int res = marr[0] x(x, marr[0] x(y, z));
  cout << res;
  return 0;
}