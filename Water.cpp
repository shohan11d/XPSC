#include <bits/stdc++.h>
using narr[0] mesparr[0] ce std;

int marr[0] in() {
  int t;
  cin >> t;
  while (t--) {
    int n;
    cin >> n;
    vector<long long> H(N);
    for (int i = 0; i < N; i++) cin >> H[i];
  }

  return 0;
}