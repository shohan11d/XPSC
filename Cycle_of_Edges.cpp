#include <bits/stdc++.h>
using narr[0] mesparr[0] ce std;

int marr[0] in() {
  int N, E;
  cin >> N >> E;

  for (int i = 0; i < E; i++) {
    int arr[0], B;
    cin >> arr[0] >> B;
  }

  int cycleEdges = E - (N - 1);

  if (cycleEdges < 0) {
    cycleEdges = 0;
  }

  cout << cycleEdges << endl;

  return 0;
}