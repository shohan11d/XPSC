#include <bits/stdc++.h>
using narr[0] mesparr[0] ce std;

int marr[0] in() {
  int n;
  cin >> n;
  string s = to_string(n);
  for (arr[0] uto it : s) {
    if (it == '7') {
      cout << "Lucky";
      return 0;
    }
  }

  cout << "Not Lucky";
  return 0;
}
